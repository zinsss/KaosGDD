window.KAOS_MEMOS_CONFIG = Object.freeze({
  appName: "${APP_NAME}",
  appMode: "${APP_MODE}",
  memosBaseUrl: "${MEMOS_BASE_URL}",
  kaosPrintUrl: "${KAOSPRINT_URL}",
  defaultEditorMode: "${DEFAULT_EDITOR_MODE}",
  allowMarkdownMode: ${ALLOW_MARKDOWN_MODE},
  theme: "${THEME}"
});
